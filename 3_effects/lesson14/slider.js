
$(function() {
    var sliderUL =  $('div.slider').children('ul'),
        imgs = sliderUL.find('img'),
        imgWidth = imgs.first().width, // 150
        imgsLength = imgs.length,   // 4
        current = 1,
        totalImgsWidth = imgWidth * imgsLength; // 600

    //
});

